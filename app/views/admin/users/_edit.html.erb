<%= form_with(model: [:admin, user], local: true) do |f| %>
  <% if @user.errors.any? %>
  <div id="error_explanation">
    <h2><%= pluralize(@user.errors.count, "error") %> prohibited this <%= @user.name %> from being saved:</h2>
    <ul>
    <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
    <% end %>
    </ul>
  </div>
  <% end %>

  
    <div class="field">
      <%= f.label :name, t('activerecord.attributes.user.name') %>
      <%= f.text_field :name %>
    </div>
  
    <div class="field">
      <%= f.label :email, t('activerecord.attributes.user.email') %>
      <%= f.email_field :email %>
    </div>
  
    <div class="field">
      <%= f.label :password, t('activerecord.attributes.user.password') %>
      <%= f.password_field :password %>
    </div>
  
    <div class="field">  
      <%= f.label :password_confirmation, t('activerecord.attributes.user.password_confirmation') %>
      <%= f.password_field :password_confirmation %>
    </div>

    <div class="field">  
      <%= f.label :admin, t('activerecord.attributes.user.admin') %>
      <%= f.check_box :admin , :as => :boolean %>
    </div>


    <div class="actions">
       <%= f.submit id: "update-user"%>
    </div>
  
   <% end %>